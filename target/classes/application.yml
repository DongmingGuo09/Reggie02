server:
  port: 8080
spring:
  application:
    name: reggie_take_away
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url:
      username: root
      password: root
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id